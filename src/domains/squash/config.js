/** Squash domain — API prefix for Phase 5 backend routes. */
export const SQUASH_DOMAIN = 'squash';
export const SQUASH_API_PREFIX = '/squash';

export const squashConfig = {
  domain: SQUASH_DOMAIN,
  apiPrefix: SQUASH_API_PREFIX,
  baseApiPath: `/api${SQUASH_API_PREFIX}`,
  mediaPrefix: 'squash/',
};
