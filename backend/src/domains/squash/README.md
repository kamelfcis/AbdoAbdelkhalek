# Squash domain (scaffold)

Phase 4 provides a placeholder router only. Full Squash CRUD, Prisma models, and `/api/squash/*` content routes are **Phase 5**.

- `routes.ts` — health stub at `/api/squash/health`
- R2 prefix `squash/` — allowlist to be added in Phase 5

See [ROADMAP.md](../../../ROADMAP.md) Phase 5.
